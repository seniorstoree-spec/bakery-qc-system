import React from 'react';

// Legacy app-wide state synchronization was removed.
// Operational records are persisted directly through the individual Supabase services.
export const RemoteDataSync: React.FC = () => null;
